It tells me what’s right and what’s not, when to leave and where to go. It’s not Shakespeare; it does not speak in memorable lines. My inner voice always gives it to me straight. Tells me who my real friends are, when to say yes, when to say no, whether the person sitting next to me is the one I’ll be spending the rest of my life with.


export CUDA_HOME=/usr/local/cuda
export PATH=$CUDA_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$LD_LIBRARY_PATH

uv add "flash-attn @ https://github.com/dao-ailab/flash-attention/releases/download/v2.7.3/flash_attn-2.7.3+cu12torch2.5cxx11abitrue-cp311-cp311-linux_x86_64.whl"

uv pip install pdfplumber nltk numpy python-docx beautifulsoup4 ebooklib pyside6 psutil piper-phonemize-fix soundfile librosa transformers lhotse tensorboard huggingface_hub pydub jieba pypinyin setuptools cn2an inflect onnxruntime onnxruntime-gpu
uv pip install ./LuxTTS/ --no-deps
uv pip install git+https://github.com/ysharma3501/LinaCodec.git --no-deps
uv pip install --upgrade k2
uv pip uninstall torch torchvision torchaudio
uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121